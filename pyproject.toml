[project]
name = "celune"
version = "0.1.0"
description = "Célune fashion discovery platform"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.57.0",
    "requests>=2.33.0",
    "urllib3>=2.6.0",
]

[tool.uv]
package = false
dev-dependencies = [
    "pytest",
]
